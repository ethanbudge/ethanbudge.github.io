Drop your CV here as `cv.pdf` (this exact filename, so `about/index.md`'s
link keeps working without edits). This folder is excluded from the Jekyll
build's markdown processing -- a PDF placed here is served as-is at
`/assets/cv/cv.pdf`.
